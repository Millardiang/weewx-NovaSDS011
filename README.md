# weewx-NovaSDS011
A **WeeWX 5.1 service** for the **Nova SDS011 particulate sensor**.   It injects real-time particulate matter values into WeeWX (`pm2_5`, `pm10_0`) and outputs a JSON snapshot (`aqi.json`) for use on dashboards or external scripts.
